<form method="post" action="{{ path('user_delete', {'id': user.id}) }}" onsubmit="return confirm('Es-tu sûr-e de vouloir supprimer ton compte ??');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="red-text text-red-accent-4 valign-wrapper"><i class="material-icons">cancel</i>Supprimer mon compte</button>
</form>
