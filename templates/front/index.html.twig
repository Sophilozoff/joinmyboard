{% extends 'layout.html.twig' %}

{% block main %}

<div class="container-fluid white section" >
    <h4 class="teal darken-1">Les prochains évènements</h4>
    <div class="row">
        {% for event in eventsNext %}
        {{ include("event/_event_block.html.twig")}}
        {% endfor %}
    </div>
</div>

<div class="container-fluid white section">
    <h4 class="teal darken-1">Les anciens évènements</h4>
    <div class="row">
        {% for event in eventsPrev %}
        {{ include("event/_event_block.html.twig")}}
        {% endfor %}
    </div>
</div>
{% endblock %}
{% block stylesheets %}
<style>
.section{
    padding: 40px 50px;
    margin-bottom: 10px;
    }
.alert-success{
        background-color: #64ffda;
        border: 2px #059481  solid;
        border-radius: 5px;
        padding: 10px;
        text-align: center;
        font-size: 1.3em;
    }
h4{
    padding: 10px 40px;
    border-radius: 25px;
    color: whitesmoke;
}
</style>
{% endblock %}